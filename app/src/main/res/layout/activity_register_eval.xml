<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".RegisterEvalActivity">

    <LinearLayout
        android:orientation="horizontal"
        android:background="@android:color/white"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_margin="10dp">

        <ImageButton
            android:id="@+id/registerEval_btn_back"
            android:stateListAnimator="@null"
            android:background="@android:color/white"
            android:maxHeight="3dp"
            android:maxWidth="3dp"
            android:padding="10dp"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/close"
            android:layout_width="?attr/actionBarSize"
            android:layout_height="match_parent"/>

        <TextView
            android:textSize="20sp"
            android:paddingStart="10dp"
            android:layout_weight="1"
            android:gravity="start|center_vertical"
            android:text="ADD EVALUATION"
            android:textColor="@color/colorblue"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"/>

        <Button
            android:id="@+id/registerEval_btn_done"
            android:stateListAnimator="@null"
            android:background="@drawable/rounding_btn_blue_circle"
            android:layout_gravity="center_vertical"
            android:text="DONE"
            android:textSize="14sp"
            android:textColor="@android:color/white"
            android:layout_width="70dp"
            android:layout_height="36dp"/>

    </LinearLayout>

    <RatingBar
        android:id="@+id/registerEval_ratingbar"
        android:layout_marginTop="10dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp"

        android:numStars="5"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <View
        android:id="@+id/divider2"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"
        android:backgroundTint="@color/colorblue"/>

    <EditText
        android:id="@+id/registerEval_edt_comment"
        android:layout_marginTop="10dp"
        android:gravity="top"
        android:padding="20dp"
        android:hint="make comment!!"
        android:scrollHorizontally="false"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="0dp"/>

</LinearLayout>
